<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.videoplayer">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <!-- Show media notification on Android 13+ -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".MainApplication"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/AppTheme"
        android:requestLegacyExternalStorage="true">

        <activity
            android:name=".MainActivity"
            android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustResize"
            android:supportsPictureInPicture="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- content:// URIs (Gallery, Downloads, Storage Access Framework) -->
            <intent-filter android:priority="1000">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <category android:name="android.intent.category.OPENABLE" />
                <data android:scheme="content" android:mimeType="video/*" />
            </intent-filter>

            <!-- file:// URIs (file managers) -->
            <intent-filter android:priority="1000">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="file" android:mimeType="video/*" />
            </intent-filter>

            <!-- http/https URIs (browsers, streaming links) -->
            <intent-filter android:priority="900">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="http"  android:mimeType="video/*" />
                <data android:scheme="https" android:mimeType="video/*" />
            </intent-filter>

            <!-- Explicit video MIME types (for apps that send exact types) -->
            <intent-filter android:priority="1000">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <category android:name="android.intent.category.OPENABLE" />
                <data android:scheme="content" android:mimeType="video/mp4" />
                <data android:scheme="content" android:mimeType="video/x-matroska" />
                <data android:scheme="content" android:mimeType="video/avi" />
                <data android:scheme="content" android:mimeType="video/quicktime" />
                <data android:scheme="content" android:mimeType="video/x-msvideo" />
                <data android:scheme="content" android:mimeType="video/webm" />
                <data android:scheme="content" android:mimeType="video/3gpp" />
                <data android:scheme="content" android:mimeType="video/3gpp2" />
                <data android:scheme="file"    android:mimeType="video/mp4" />
                <data android:scheme="file"    android:mimeType="video/x-matroska" />
                <data android:scheme="file"    android:mimeType="video/avi" />
                <data android:scheme="file"    android:mimeType="video/quicktime" />
                <data android:scheme="file"    android:mimeType="video/x-msvideo" />
                <data android:scheme="file"    android:mimeType="video/webm" />
                <data android:scheme="file"    android:mimeType="video/3gpp" />
                <data android:scheme="file"    android:mimeType="video/3gpp2" />
            </intent-filter>
        </activity>

        <!-- Receives play/pause intent from the PiP floating window button -->
        <receiver
            android:name=".PipActionReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.videoplayer.PIP_PLAY_PAUSE" />
            </intent-filter>
        </receiver>

        <!-- Receives play/pause / stop from the media notification -->
        <receiver
            android:name=".MediaNotificationReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.videoplayer.NOTIF_PLAY_PAUSE" />
                <action android:name="com.videoplayer.NOTIF_STOP" />
            </intent-filter>
        </receiver>
    </application>

</manifest>